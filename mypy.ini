[mypy]
python_version = 3.11
# Optional third-party libs (pypdf, python-docx) are imported lazily; don't
# fail when their stubs aren't installed in CI.
ignore_missing_imports = True
warn_unused_ignores = True
no_implicit_optional = True
warn_redundant_casts = True
