# markdownlint config used by Super-Linter (VALIDATE_MARKDOWN).
default: true
MD013: false   # line length — prose/tables wrap naturally
MD033: false   # inline HTML — allowed in docs
MD041: false   # first line need not be a top-level heading
MD024:
  siblings_only: true   # allow repeated headings under different sections
